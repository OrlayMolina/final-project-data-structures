/**
 * Defines an interface representing a person.
 */

export interface IPerson {
    name: string;
    lastName: string;
    ID: string;
    address: string;
    userName: string;
    password: string;
}